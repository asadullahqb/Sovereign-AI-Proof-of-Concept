version: "3.9"
services:
  app:
    build: .
    image: ytl-ai-labs-poc:latest
    container_name: ytl-ai-labs-poc
    ports:
      - "8501:8501"
    env_file:
      - .env
    volumes:
      - .:/app
    command: python -c "print('Override to run Streamlit when ready')"
